{
  "name": "levit",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@levit/web",
    "dev:api": "turbo run dev --filter=@levit/api",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check:types": "turbo run check:types"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
