# dependencies
node_modules/
.pnpm-store/

# turbo
.turbo/

# build output
dist/
.next/
out/
build/

# test / coverage
coverage/
test-results/
playwright-report/
.playwright-mcp/

# prisma generated client (regenerated on install)
apps/api/generated/
# crawler output (regenerated by `pnpm --filter @levit/api crawl`)
apps/api/prisma/seed/products.crawled.json

# local databases (PGlite)
*.db
apps/web/local.db*

# env (keep .env.example committed)
.env.local
*.env.local
.env*.local
# api .env holds the Supabase secret — keep it out of git
apps/api/.env
# direnv — holds the Supabase MCP access token (PAT)
.envrc

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# git worktrees
.worktrees/
